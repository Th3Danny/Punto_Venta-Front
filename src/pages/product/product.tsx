import { Box, Typography } from '@mui/material';

const Product = () => (
  <Box sx={{ padding: 4 }}>
    <Typography variant="h4" gutterBottom>Productos</Typography>
    <Typography>Vista de administración de productos. Acceso: MANAGER, ADMIN.</Typography>
  </Box>
);

export default Product;
