import { Box, Typography } from '@mui/material';

const Users = () => (
  <Box sx={{ padding: 4 }}>
    <Typography variant="h4" gutterBottom>Usuarios</Typography>
    <Typography>Administración de usuarios. Acceso: ADMIN.</Typography>
  </Box>
);

export default Users;
