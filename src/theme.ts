import { createTheme } from '@mui/material';

export const theme = createTheme({
    typography: {
        button: {
            textTransform: 'none',
            width: 'auto',
            height: 'auto'
        }
    }
});

export default theme;